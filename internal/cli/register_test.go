package cli

// Blank-import the full ruleset so tests that exercise `rules`
// subcommand and its registry lookups see the real list of rules.
import _ "github.com/donaldgifford/claudelint/internal/rules/all"
